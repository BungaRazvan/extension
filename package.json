{
  "name": "extension",
  "version": "1.0.0",
  "main": "background.js",
  "scripts": {
    "copy:css": "cp -r src/css dist/",
    "copy:man": "cp -r manifest.json dist/",
    "build": "npm run copy:man && run copy:css && esbuild src/*.js --bundle --outdir=dist",
    "watch": "npm run copy:man && npm run copy:css && esbuild src/*.js --bundle --outdir=dist --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "esbuild": "^0.25.10"
  }
}
